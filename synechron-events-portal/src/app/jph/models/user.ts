import { Address } from "./address";
import { Company } from "./company";

export class User {
    constructor(userName: string, address: Address, phone: string, website: string, company: Company) { }
}